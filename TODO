ToDo
====

  1. Allow for scanning the repository to see for which packages there are
     newer versions in the AUR. (This could be complicated, but would be
     very very useful.) (syntax: repo sync)
